# SVN文件搜索系统部署文档

## 1. 环境要求

### 1.1 后端环境

| 软件/依赖 | 版本/要求 | 用途 | 安装命令 |
|-----------|-----------|------|----------|
| Python | 3.10+ | 开发语言 | [Python官网下载](https://www.python.org/downloads/) |
| SVN命令行客户端 | 1.10+ | SVN操作 | [TortoiseSVN官网下载](https://tortoisesvn.net/downloads.html) |
| FastAPI | 0.104.1 | Web框架 | `pip install fastapi` |
| SQLAlchemy | 2.0.23 | ORM | `pip install sqlalchemy` |
| uvicorn | 0.23.2 | ASGI服务器 | `pip install uvicorn` |
| websockets | 11.0.3 | WebSocket支持 | `pip install websockets` |

### 1.2 前端环境

| 软件/依赖 | 版本/要求 | 用途 | 安装命令 |
|-----------|-----------|------|----------|
| Node.js | 18+ | JavaScript运行环境 | [Node.js官网下载](https://nodejs.org/en/download/) |
| npm | 9+ | 包管理器 | 随Node.js一起安装 |
| Vue3 | 3.3.8 | 前端框架 | `npm install vue` |
| Ant Design Vue | 4.0.0 | UI库 | `npm install ant-design-vue` |
| Vite | 5.0.0 | 构建工具 | `npm install vite` |

## 2. 快速部署（推荐）

### 2.1 一键部署脚本

项目提供了一键部署脚本，自动安装依赖并初始化数据库。

**Windows:**
```bash
deploy.bat
```

**Linux/Mac:**
```bash
chmod +x deploy.sh
./deploy.sh
```

### 2.2 一键启动/停止脚本

**Windows:**
```bash
# 启动服务
restart.bat

# 停止服务
stop.bat
```

**Linux/Mac:**
```bash
# 启动服务
chmod +x restart.sh
./restart.sh

# 停止服务
chmod +x stop.sh
./stop.sh
```

## 3. 手动部署步骤

### 3.1 后端部署

1. **克隆代码**
   ```bash
   git clone <repository-url>
   cd search-svn/backend
   ```

2. **安装依赖**
   ```bash
   pip install -r requirements.txt
   ```

3. **启动服务**
   ```bash
   python main.py
   ```
   服务默认运行在 `http://0.0.0.0:8001`

### 3.2 前端部署

1. **进入前端目录**
   ```bash
   cd search-svn/frontend
   ```

2. **安装依赖**
   ```bash
   npm install
   ```

3. **开发模式**
   ```bash
   npm run dev
   ```
   前端默认运行在 `http://localhost:5173`

4. **生产构建**
   ```bash
   npm run build
   ```
   构建产物将生成在 `dist` 目录

## 4. 端口配置

项目支持通过环境变量和 `.env` 文件灵活配置前后端端口。

### 4.1 配置文件

修改 `frontend/.env` 文件：

```bash
# 后端服务地址配置
VITE_BACKEND_HOST=localhost
VITE_BACKEND_PORT=8001

# 前端开发服务器端口
FRONTEND_PORT=5173
```

### 4.2 配置优先级

1. **系统环境变量**（最高优先级）
   - Windows: `set BACKEND_PORT_ENV=8001`
   - Linux/Mac: `export BACKEND_PORT_ENV=8001`

2. **`.env` 文件**（推荐）
   - 修改 `frontend/.env` 文件中的配置

3. **默认值**（最低优先级）
   - 后端：8001
   - 前端：5173

### 4.3 使用环境变量启动

**Windows:**
```bash
set BACKEND_PORT_ENV=8001
set FRONTEND_PORT_ENV=5173
restart.bat
```

**Linux/Mac:**
```bash
BACKEND_PORT_ENV=8001 FRONTEND_PORT_ENV=5173 ./restart.sh
```

## 5. 其他配置

### 5.1 后端配置

1. **数据库配置**
   - 默认数据库：`db.sqlite3`
   - 修改方法：修改 `app/models/database.py` 文件中的 `SQLALCHEMY_DATABASE_URL`

2. **CORS配置**
   - 默认允许所有跨域请求
   - 修改方法：修改 `main.py` 文件中的 `CORSMiddleware` 配置

### 5.2 前端配置

1. **API地址配置**
   - 配置文件：`frontend/src/config/api.ts`
   - 自动从 `.env` 文件读取 `VITE_BACKEND_HOST` 和 `VITE_BACKEND_PORT`

2. **WebSocket地址配置**
   - 与API地址配置相同，自动从 `.env` 文件读取

## 6. 服务管理

### 6.1 使用脚本管理服务

**查看运行状态:**
```bash
# Windows
netstat -ano | findstr ":8001"
netstat -ano | findstr ":5173"

# Linux/Mac
lsof -Pi :8001 -sTCP:LISTEN
lsof -Pi :5173 -sTCP:LISTEN
```

**查看日志:**
```bash
# 后端日志
tail -f backend/backend.log

# 前端日志
tail -f frontend/frontend.log
```

### 6.2 手动管理服务

1. **后端服务**
   ```bash
   # 开发模式
   python main.py
   
   # 生产模式（使用uvicorn）
   uvicorn main:app --host 0.0.0.0 --port 8001
   ```

2. **前端服务**
   ```bash
   # 开发模式
   npm run dev
   
   # 生产模式（使用静态文件服务器）
   npm install -g serve
   serve -s dist
   ```

## 7. 常见问题及解决方案

### 7.1 后端问题

1. **SVN命令未找到**
   - 症状：扫描任务失败，提示 "svn: command not found"
   - 解决方案：安装SVN命令行客户端，并将其添加到系统PATH环境变量

2. **数据库连接失败**
   - 症状：启动服务失败，提示数据库连接错误
   - 解决方案：确保SQLite数据库文件有写入权限

3. **端口被占用**
   - 症状：启动服务失败，提示 "Address already in use"
   - 解决方案：修改端口配置，或关闭占用端口的进程

### 7.2 前端问题

1. **API请求失败**
   - 症状：前端无法连接到后端API
   - 解决方案：确保后端服务已启动，且API地址配置正确

2. **WebSocket连接失败**
   - 症状：扫描进度无法实时更新
   - 解决方案：确保后端WebSocket服务已启动，且WebSocket地址配置正确

3. **构建失败**
   - 症状：`npm run build` 命令失败
   - 解决方案：检查依赖是否安装正确，或尝试删除 `node_modules` 目录后重新安装



## 8. 性能优化

### 8.1 后端优化

1. **批处理大小**
   - 默认批处理大小：100
   - 可以在SVN配置页面调整批处理大小，根据系统性能进行优化

2. **数据库优化**
   - 使用SQLite的FTS5虚拟表进行全文搜索
   - 定期清理数据库，删除旧数据

### 8.2 前端优化

1. **分页查询**
   - 默认分页大小：10
   - 可以根据需要调整分页大小，平衡性能和用户体验

2. **缓存策略**
   - 对频繁访问的数据进行缓存，减少API请求

## 9. 安全注意事项

1. **密码存储**
   - 目前密码以明文形式存储在数据库中
   - 建议在生产环境中使用加密存储

2. **API访问控制**
   - 目前API没有访问控制
   - 建议在生产环境中添加认证和授权机制

3. **CORS配置**
   - 目前允许所有跨域请求
   - 建议在生产环境中限制允许的域名

## 10. 升级与维护

### 10.1 升级步骤

1. **备份数据**
   - 备份数据库文件 `db.sqlite3`

2. **更新代码**
   - 拉取最新代码
   - 安装新的依赖

3. **重启服务**
   - 停止旧服务
   - 启动新服务

### 10.2 维护计划

1. **定期备份**
   - 定期备份数据库文件

2. **日志监控**
   - 监控服务日志，及时发现和解决问题

3. **性能监控**
   - 监控服务性能，及时优化

## 11. 总结

本部署文档提供了SVN文件搜索系统的详细部署步骤、环境要求和配置说明。通过按照文档中的步骤进行部署，可以快速搭建和运行SVN文件搜索系统。

系统的部署过程相对简单，主要包括安装依赖、启动服务等步骤。在部署过程中，需要注意环境要求和常见问题的解决方案，确保系统能够正常运行。

同时，为了保证系统的安全性和性能，建议在生产环境中进行适当的配置和优化。