# SVN文件搜索系统设计文档

## 1. 系统架构

### 1.1 整体架构

本系统采用前后端分离的架构，后端使用FastAPI框架提供API服务，前端使用Vue3框架构建用户界面。系统分为以下几个模块：

1. **后端模块**
   - API层：处理HTTP请求和WebSocket连接
   - 服务层：实现核心业务逻辑
   - 数据层：处理数据库操作

2. **前端模块**
   - 视图层：负责用户界面展示
   - 逻辑层：处理前端业务逻辑
   - 通信层：与后端API交互

### 1.2 技术栈

| 类别 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 后端 | Python | 3.10+ | 开发语言 |
| 后端 | FastAPI | 0.104.1 | Web框架 |
| 后端 | SQLAlchemy | 2.0.23 | ORM |
| 后端 | SQLite | 3.40.0 | 数据库 |
| 后端 | WebSocket | - | 实时通信 |
| 前端 | Vue3 | 3.3.8 | 前端框架 |
| 前端 | Ant Design Vue | 4.0.0 | UI库 |
| 前端 | Vite | 5.0.0 | 构建工具 |

## 2. 目录结构

### 2.1 后端目录结构

```
backend/
├── app/
│   ├── api/
│   │   └── routes.py        # API路由定义
│   ├── models/
│   │   └── database.py      # 数据库模型
│   └── services/
│       └── svn_scanner.py   # SVN扫描服务
├── main.py                  # 应用入口
├── requirements.txt         # 依赖声明
└── db.sqlite3               # 数据库文件
```

### 2.2 前端目录结构

```
frontend/
├── src/
│   ├── views/
│   │   ├── SVNConfig.vue    # SVN配置页面
│   │   ├── FileScan.vue     # 文件扫描页面
│   │   └── FileSearch.vue   # 文件搜索页面
│   ├── App.vue              # 应用主组件
│   └── main.js              # 应用入口
├── package.json             # 依赖声明
└── index.html               # HTML模板
```

## 3. 核心模块设计

### 3.1 后端模块

#### 3.1.1 API层

API层负责处理HTTP请求和WebSocket连接，提供以下接口：

| 接口 | 方法 | 路径 | 功能 |
|------|------|------|------|
| 保存配置 | POST | /api/config/save | 保存SVN配置 |
| 获取配置 | GET | /api/config/get | 获取SVN配置 |
| 测试连接 | GET | /api/config/test | 测试SVN连接 |
| 新建任务 | POST | /api/task/new | 创建新的扫描任务 |
| 继续任务 | POST | /api/task/resume | 继续扫描任务 |
| 暂停任务 | POST | /api/task/pause | 暂停扫描任务 |
| 获取任务状态 | GET | /api/task/status | 获取任务状态 |
| WebSocket | WebSocket | /api/ws/progress | 实时获取扫描进度 |
| 搜索文件 | GET | /api/file/search | 搜索文件 |
| 下载文件 | GET | /api/file/download | 下载文件 |

#### 3.1.2 服务层

服务层实现核心业务逻辑，主要包括：

1. **SVNScanner**：负责扫描SVN仓库，支持断点续扫和暂停功能。
   - `scan()`：开始扫描任务
   - `pause()`：暂停扫描任务
   - `stop()`：停止扫描任务

#### 3.1.3 数据层

数据层处理数据库操作，主要包括：

1. **ScanTask**：扫描任务模型，存储任务状态和配置。
2. **SVNFile**：文件模型，存储文件信息。
3. **FTS5虚拟表**：用于全文搜索。

### 3.2 前端模块

#### 3.2.1 视图层

视图层负责用户界面展示，主要包括：

1. **SVNConfig**：SVN配置页面，用于配置SVN仓库地址、用户名、密码等。
2. **FileScan**：文件扫描页面，用于启动、暂停和继续扫描任务，实时显示扫描进度和日志。
3. **FileSearch**：文件搜索页面，用于搜索文件，支持全文搜索和分页。

#### 3.2.2 逻辑层

逻辑层处理前端业务逻辑，主要包括：

1. **配置管理**：处理SVN配置的保存、获取和测试。
2. **扫描管理**：处理扫描任务的创建、暂停和继续。
3. **搜索管理**：处理文件搜索和结果展示。
4. **下载管理**：处理文件下载和链接复制。

#### 3.2.3 通信层

通信层与后端API交互，主要包括：

1. **HTTP请求**：处理配置管理、任务管理和搜索请求。
2. **WebSocket连接**：处理实时扫描进度和日志。

## 4. 数据库设计

### 4.1 表结构

#### 4.1.1 ScanTask表

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | INTEGER | PRIMARY KEY | 任务ID |
| root_svn_url | TEXT | NOT NULL | SVN仓库地址 |
| username | TEXT | NOT NULL | 用户名 |
| password | TEXT | NOT NULL | 密码 |
| status | TEXT | NOT NULL | 任务状态 |
| scanned_count | INTEGER | DEFAULT 0 | 已扫描文件数 |
| total_count | INTEGER | DEFAULT 0 | 总文件数 |
| batch_size | INTEGER | DEFAULT 500 | 批处理大小 |
| now_scan_dir | TEXT | DEFAULT NULL | 当前扫描目录 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

#### 4.1.2 SVNFile表

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | INTEGER | PRIMARY KEY | 文件ID |
| task_id | INTEGER | REFERENCES ScanTask(id) | 任务ID |
| file_name | TEXT | NOT NULL | 文件名 |
| file_path | TEXT | NOT NULL | 文件路径 |
| svn_url | TEXT | NOT NULL | SVN链接 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 4.1.3 FTS5虚拟表

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|---------|------|------|
| id | INTEGER | PRIMARY KEY | 文件ID |
| file_name | TEXT | NOT NULL | 文件名 |
| file_path | TEXT | NOT NULL | 文件路径 |

## 5. 核心功能实现

### 5.1 SVN配置管理

1. **保存配置**：将SVN配置保存到数据库，创建一个新的扫描任务。
2. **获取配置**：从数据库获取最新的SVN配置。
3. **测试连接**：使用SVN命令测试连接是否成功。

### 5.2 文件扫描

1. **新建任务**：创建一个新的扫描任务，清空旧数据。
2. **开始扫描**：使用SVN命令扫描仓库，实时更新扫描进度。
3. **暂停扫描**：暂停扫描任务，保存当前扫描目录。
4. **继续扫描**：从上次保存的目录继续扫描。
5. **断点续扫**：当扫描过程中断后，重新启动时从上次保存的目录继续扫描。

### 5.3 文件搜索

1. **全文搜索**：使用FTS5虚拟表进行全文搜索。
2. **空关键字查询**：当关键字为空时，查询所有文件。
3. **分页查询**：支持分页查询，提高性能。

### 5.4 文件下载

1. **直接下载**：使用SVN命令下载文件，返回给前端。
2. **复制链接**：复制文件的SVN链接到剪贴板。

## 6. 技术难点及解决方案

### 6.1 断点续扫

**难点**：如何在扫描过程中断后，从上次的位置继续扫描。

**解决方案**：
- 在扫描过程中，实时保存当前扫描的目录。
- 当扫描任务暂停或中断时，保存当前扫描目录。
- 当继续扫描时，从上次保存的目录开始扫描。
- 当当前目录扫描完成后，自动从父目录继续扫描，确保所有目录都被扫描。

### 6.2 实时进度更新

**难点**：如何实时向前端推送扫描进度和日志。

**解决方案**：
- 使用WebSocket建立实时通信通道。
- 在扫描过程中，定期向前端推送扫描进度和日志。
- 前端接收到进度更新后，实时更新界面。

### 6.3 全文搜索

**难点**：如何实现高效的全文搜索。

**解决方案**：
- 使用SQLite的FTS5虚拟表进行全文搜索。
- 在扫描过程中，将文件信息添加到FTS5虚拟表。
- 当用户搜索时，使用FTS5的MATCH操作符进行全文搜索。

### 6.4 文件下载

**难点**：如何实现无需输入用户名密码的文件下载。

**解决方案**：
- 在后端存储SVN配置（包括用户名和密码）。
- 当用户请求下载文件时，后端使用存储的配置进行认证。
- 使用SVN命令下载文件，然后返回给前端。

## 7. 部署方案

### 7.1 环境要求

1. **后端环境**
   - Python 3.10+
   - SVN命令行客户端
   - 依赖包：FastAPI、SQLAlchemy、uvicorn

2. **前端环境**
   - Node.js 18+
   - npm 9+

### 7.2 部署步骤

1. **后端部署**
   - 安装依赖：`pip install -r requirements.txt`
   - 启动服务：`python main.py`

2. **前端部署**
   - 安装依赖：`npm install`
   - 开发模式：`npm run dev`
   - 生产构建：`npm run build`

### 7.3 配置说明

1. **后端配置**
   - 端口：默认8001
   - 数据库：默认使用db.sqlite3

2. **前端配置**
   - API地址：默认http://localhost:8001/api
   - WebSocket地址：默认ws://localhost:8001/api/ws/progress

## 8. 测试计划

### 8.1 功能测试

1. **SVN配置测试**
   - 测试配置保存功能
   - 测试连接测试功能

2. **文件扫描测试**
   - 测试新建任务功能
   - 测试暂停和继续功能
   - 测试断点续扫功能

3. **文件搜索测试**
   - 测试全文搜索功能
   - 测试空关键字查询功能
   - 测试分页查询功能

4. **文件下载测试**
   - 测试直接下载功能
   - 测试复制链接功能

### 8.2 性能测试

1. **扫描性能测试**
   - 测试扫描10000个文件的时间

2. **搜索性能测试**
   - 测试搜索响应时间

### 8.3 可靠性测试

1. **断点续扫测试**
   - 测试扫描过程中断后，是否能够继续扫描

2. **错误处理测试**
   - 测试SVN连接错误处理
   - 测试认证错误处理

## 9. 总结

本系统采用前后端分离的架构，使用FastAPI和Vue3技术栈，实现了SVN文件搜索系统的核心功能。系统支持SVN配置管理、文件扫描、文件搜索和文件下载等功能，能够满足开发人员和项目管理人员的日常需求。

系统的主要特点包括：
- 支持断点续扫，确保扫描过程中断后可以继续
- 实时显示扫描进度和日志，提高用户体验
- 支持全文搜索，快速查找文件
- 支持直接下载文件，无需输入用户名密码
- 支持空关键字查询，查询所有文件

系统的技术难点包括断点续扫、实时进度更新、全文搜索和文件下载，通过合理的设计和实现，这些问题都得到了有效的解决。

本系统可以部署在本地环境中，为开发团队提供便捷的SVN文件搜索和管理工具。